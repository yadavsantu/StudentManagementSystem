<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <title>Edit Student Page</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f0f2f5;
            margin: 0;
            padding: 0;
        }

        .edit-student {
            width: 50%;
            margin: 40px auto;
            background-color: #ffffff;
            padding: 40px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            box-sizing: border-box;
        }

        h2 {
            text-align: center;
            color: #343a40;
            margin-bottom: 20px;
        }

        p {
            text-align: center;
            color: #6c757d;
            font-size: 16px;
            margin-bottom: 30px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-control {
            padding: 10px;
            font-size: 16px;
            border-radius: 5px;
            border: 1px solid #ced4da;
        }

        .form-control:focus {
            border-color: #80bdff;
            box-shadow: 0 0 0 0.25rem rgba(38, 143, 255, 0.25);
        }

        .btn-custom {
            background-color: #007bff;
            color: white;
            padding: 10px 15px;
            font-size: 16px;
            border: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            width: 100%;
        }

        .btn-custom:hover {
            background-color: #0056b3;
        }

        .back-link {
            text-decoration: none;
            color: #007bff;
            font-size: 14px;
            display: block;
            text-align: center;
            margin-top: 15px;
        }

        .back-link:hover {
            text-decoration: underline;
        }

        .student-image-preview {
            margin-top: 10px;
            max-width: 100px;
            max-height: 100px;
            object-fit: cover;
        }

        @media (max-width: 768px) {
            .edit-student {
                width: 80%;
            }
        }
    </style>
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="edit-student">
        <h2>Edit Student</h2>
        <p>Please update the student's information below</p>

        <form action="/update-student/<%= data.id %>" method="post" enctype="multipart/form-data">
            <div class="form-group">
                <input type="text" class="form-control" placeholder="Student Name" name="std_name" value="<%= data.std_name %>">
            </div>
            <div class="form-group">
                <input type="text" class="form-control" placeholder="Student Address" name="std_address" value="<%= data.std_address %>">
            </div>
            <div class="form-group">
                <input type="text" class="form-control" placeholder="Student Age" name="std_age" value="<%= data.std_age %>">
            </div>
            <div class="form-group">
                <input type="text" class="form-control" placeholder="Student Fees" name="std_fees" value="<%= data.std_fees %>">
            </div>
            <div class="form-group">
                <input type="text" class="form-control" placeholder="Student Level" name="std_level" value="<%= data.std_level %>">
            </div>

            <!-- Image Upload Field -->
            <div class="form-group">
                <label for="std_image">Upload New Image</label>
                <input type="file" class="form-control" name="std_image" accept="image/*">
                <% if (data.std_image) { %>
                    <p>Current Image:</p>
                    <img src="/uploads/<%= data.std_image %>" alt="Current Student Image" class="student-image-preview">
                <% } %>
            </div>

            <button type="submit" class="btn-custom">Update the Data</button>
        </form>

        <a href="/all-students" class="back-link">Back to All Students</a>
    </div>

    <%- include('../partials/footer') %>
</body>
</html>
